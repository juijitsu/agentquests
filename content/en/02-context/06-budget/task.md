# Papers · An expensive history and a cheap bridge

Five blocks, all relevant. The budget is eighty units, and all of it together
costs a hundred and sixty.

The agent builds the brief from fresh to old and answers: found no limits.

```
python engine/check.py content/en/02-context/06-budget/starter/novice/agent.ts
```

Look at the second check line: the `bridge-limit` block never made the brief. The
bridge limit was set in twenty nineteen — it is the oldest thing in the pile and
the only thing that answers the question. Today's weather is seven years fresher
and settles nothing.

**Select by return per unit of cost.**

Careful with the obvious fix: the most valuable block here is the bridge repair
history, and it costs seventy out of eighty. Take it and you crowd out the two
cheap blocks that give the answer.

## Done when

- the budget is not exceeded;
- the brief holds both decisive blocks — the limit and the weight;
- the agent answers that the load will not pass;
- no more than two calls to the model.

## Choose a difficulty

| Folder | What you get |
|---|---|
| `starter/novice/` | selection by age, with the right quantity named in the `TODO` |
| `starter/advanced/` | only a note that everything is relevant and the budget is short |
| `starter/pro/` | the contract and both traps named outright |

## If you get stuck

Work out the return of two blocks on paper: the limit is nine for twenty, the
repair history is ten for seventy. Which is the better one to take first?
