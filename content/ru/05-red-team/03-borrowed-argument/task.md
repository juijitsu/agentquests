# Красная команда · В промпте чисто

Поверхность входов у вас есть с первого уровня, работающие примечания — со
второго. Вы спустились на уровень вызовов и разобрали один вызов агента по
заявке L-4471: шесть аргументов.

Вы проверили те, которые модель написала сама, и нашли один опасный: текст
уведомления собран из примечания грузоотправителя. Остальные модель не
писала, их подставил код, и промпт через них не проходит.

```
python engine/check.py content/ru/05-red-team/03-borrowed-argument/starter/novice/agent.py
```

Посмотрите на строку про пропущенные. Их два. Оба подставил код: один взял
адрес из контакта получателя, другой номер из документа брокера. Оба поля
заполняются снаружи, без вашего пароля.

Промпт был чист. Через него эти значения и не шли.

**Найдите аргументы, значение которых приходит из входа снаружи.**

## Сдано, если

- прослежен каждый аргумент вызова;
- в отчёт попали все аргументы из поверхности;
- лишних в отчёте нет;
- заявленное число обращений совпадает со сделанным.

## Выберите сложность

| Папка | Что дано |
|---|---|
| `starter/novice/` | отбор по автору строки, в `TODO` назван нужный признак |
| `starter/advanced/` | отбор по автору строки, без подсказки |
| `starter/pro/` | контракт и определение аргумента из поверхности |

## Если застряли

Вопрос не «кто вписал значение», а «чьи руки набрали то, что в нём
оказалось». Путь через код короче пути через модель и не проходит ни одной
защиты промпта.
