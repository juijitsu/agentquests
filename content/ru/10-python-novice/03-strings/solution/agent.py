"""Уровень 03 · эталон."""


def solve(text: str) -> str:
    clean = text.strip()         # новая строка без пробелов и переводов строк
    code = clean.upper()         # ещё одна новая строка, уже заглавными
    return code[:3] + code[-2:]  # три первых символа и два последних
