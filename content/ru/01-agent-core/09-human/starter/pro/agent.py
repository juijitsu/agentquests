"""Уровень 09 · профессионал.

Контракт:
    run() -> tuple[str, int]

Доступно:
    run_tool("act", {"name": ...})  -> выполняет действие
    run_tool("ask", {"name": ...})  -> отправляет на согласование, вернёт вердикт
    REFUSED — список отклонённых, модель в него смотрит

Модель кладёт в аргументы вызова поле irreversible. Согласующий — живой
человек на смене, и его внимание кончается раньше, чем ваши вопросы.
Отказ обязан останавливать действие, иначе согласование бессмысленно.
"""

from scenario import REFUSED, Model, TOOLS, run_tool


def run() -> tuple[str, int]:
    raise NotImplementedError
