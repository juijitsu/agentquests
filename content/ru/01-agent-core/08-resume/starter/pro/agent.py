"""Уровень 08 · профессионал.

Контракт:
    run() -> tuple[str, int]

run() будет вызвана дважды: первый заход прервётся на середине, второй
обязан довести рейс до конца. Бронь платная и необратимая — перегон,
оплаченный дважды, засчитан не будет.

Доступно:
    DONE — список, переживающий перезапуск (в жизни это файл или строка в БД)

Модель смотрит в DONE, а не в историю разговора. Крах наследуется от
BaseException: перехватить его нельзя, на то он и крах.
"""

from scenario import DONE, Model, TOOLS, run_tool


def run() -> tuple[str, int]:
    raise NotImplementedError
