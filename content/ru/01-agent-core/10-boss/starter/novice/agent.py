"""Уровень 10 · новичок. Три нарушения на виду, четвёртое появится при починке."""

from scenario import APPROVED, EMPTY, Model, TOOLS, run_tool

MAX_STEPS = 12


def run() -> tuple[str, int]:
    model = Model()
    step = 0

    # TODO 1 (уровень 07): это снимок очереди на момент побудки. Задача,
    #        которая родится по ходу смены, в него не попадёт. Перечитывайте
    #        очередь на каждом круге и выходите по EMPTY.
    for action in run_tool("pending", {}).split(" | "):
        step += 1

        # TODO 2 (уровень 09): человека беспокоят по любому поводу. Спрашивать
        #        нужно только про необратимое — model.judge(action) это скажет.
        # TODO 3 (уровень 08): согласование знали только вы, и оно умрёт вместе
        #        с процессом. Отметьте его в APPROVED и не спрашивайте дважды.
        run_tool("ask", {"name": action})

        run_tool("handle", {"name": action})

    # TODO 4 (уровень 02): сейчас цикл конечен сам собой — снимок не растёт.
    #        Как только вы замените его живым чтением, конечность станет вашей
    #        обязанностью: ограничитель из уровня 02 понадобится снова.
    return model.close(), step
